<Application x:Class="ProductHierarchyHelper.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ProductHierarchyHelper"
             xmlns:sys="clr-namespace:System.Windows;assembly=PresentationFramework"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <sys:Thickness x:Key="MarginSize" Bottom="5" Left="5" Top="5" Right="5" />
        <Style x:Key="BaseStyle" TargetType="FrameworkElement">
            <Setter Property="Margin" Value="{StaticResource MarginSize}" />
        </Style>
        <Style TargetType="GroupBox" BasedOn="{StaticResource BaseStyle}" />
        <Style TargetType="ListBox" BasedOn="{StaticResource BaseStyle}" />
        <Style TargetType="Button" BasedOn="{StaticResource BaseStyle}">
            <Setter Property="Padding" Value="{StaticResource MarginSize}" />
            <Setter Property="MinWidth" Value="70" />
        </Style>
		<Style TargetType="ComboBox" BasedOn="{StaticResource BaseStyle}" />
		<Style TargetType="TextBox" BasedOn="{StaticResource BaseStyle}">
			<Setter Property="Padding" Value="{StaticResource MarginSize}" />
		</Style>
	</Application.Resources>
</Application>
